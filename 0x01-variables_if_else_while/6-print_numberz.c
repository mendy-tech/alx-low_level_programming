#include <stdio.h>

/**
 * main - prints number from 0 to 9 of base 10
 * followed by a new line
 *
 * Return: Always 0.
 */
int main(void)
{
	int ch;

	for (ch = 48; ch <= 59; ch++)
	{
		putchar(ch);
	}
	putchar(10);
		return (0);
}
