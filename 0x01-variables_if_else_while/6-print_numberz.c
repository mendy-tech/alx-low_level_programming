#include <stdio.h>

/**
 * main - prints number from 0 to 9 of base 10
 * followed by a new line
 *
 * Return: Always 0.
 */
int main(void)
{
	int num;

	for (num = 48; num <= 59; num++)
	{
		putchar(num);
	}
	putchar(10);
		return (0);
}
